#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
    cin.tie(nullptr)->sync_with_stdio(false);

    int n; cin >> n;

    for (int i = 1; i <= n; i++) {
        cout << i << ' ';
    }
    for (int i = 1; i <= n; i++) {
        cout << i << ' ';
    }
    for (int i = 1; i <= n; i++) {
        cout << i << ' ';
    }
}