#include <iostream>
using namespace std;

int main() {
	int a, b, c, d;
	scanf("%d %d %d %d", &a, &b, &c, &d);

	if (a != 8 && a != 9) printf("answer");
	else if (d != 8 && d != 9) printf("answer");
	else if (b != c) printf("answer");
	else printf("ignore");

	return 0;
}
/*
문제 해법		: 
접근 방식		:
결정적 깨달음		:
오답 원인		: 1. 
				  2.
*/
//////////////////////////////////////////////////////////////////////