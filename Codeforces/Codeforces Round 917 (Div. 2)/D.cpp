// a_{ik + j} = p_i * 2^{q_j}

// case 1:
// a_{xk + y} > a_{uk + v} (x = u and y < v)

// case 2:
// a_{xk + y} > a_{uk + v} (x < u)

#include <bits/stdc++.h>
using namespace std;

const int MOD = 998'244'353;

int main() {
    #ifndef ONLINE_JUDGE
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "r", stdin);
    #endif

    cin.tie(NULL); cout.tie(NULL);
    ios_base::sync_with_stdio(false);

    
}